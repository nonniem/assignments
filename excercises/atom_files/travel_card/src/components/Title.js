import React from "react"

function Title(){
  const styling = {
    textAlign: "center",
    background: "blue"
  }
  return(
    <div>
        <h1 style={styling}>Vacation Destinations around the World</h1>
    </div>
  )};

  export default Title;
